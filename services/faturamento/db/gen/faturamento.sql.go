// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: faturamento.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const adicionarItemNota = `-- name: AdicionarItemNota :one
INSERT INTO faturamento.nota_items (
    nota_id, 
    produto_id, 
    quantidade
) VALUES (
    $1, $2, $3
)
RETURNING id, nota_id, produto_id, quantidade
`

type AdicionarItemNotaParams struct {
	NotaID     pgtype.UUID `json:"nota_id"`
	ProdutoID  pgtype.UUID `json:"produto_id"`
	Quantidade int32       `json:"quantidade"`
}

func (q *Queries) AdicionarItemNota(ctx context.Context, arg AdicionarItemNotaParams) (FaturamentoNotaItem, error) {
	row := q.db.QueryRow(ctx, adicionarItemNota, arg.NotaID, arg.ProdutoID, arg.Quantidade)
	var i FaturamentoNotaItem
	err := row.Scan(
		&i.ID,
		&i.NotaID,
		&i.ProdutoID,
		&i.Quantidade,
	)
	return i, err
}

const buscarItemsNota = `-- name: BuscarItemsNota :many
SELECT 
    id, 
    nota_id, 
    produto_id, 
    quantidade
FROM faturamento.nota_items
WHERE nota_id = $1
ORDER BY id
`

func (q *Queries) BuscarItemsNota(ctx context.Context, notaID pgtype.UUID) ([]FaturamentoNotaItem, error) {
	rows, err := q.db.Query(ctx, buscarItemsNota, notaID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FaturamentoNotaItem{}
	for rows.Next() {
		var i FaturamentoNotaItem
		if err := rows.Scan(
			&i.ID,
			&i.NotaID,
			&i.ProdutoID,
			&i.Quantidade,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const buscarNotaPorId = `-- name: BuscarNotaPorId :one
SELECT id, num_seq, status, created_at, printed_at FROM faturamento.notas 
WHERE id = $1
`

func (q *Queries) BuscarNotaPorId(ctx context.Context, id pgtype.UUID) (FaturamentoNota, error) {
	row := q.db.QueryRow(ctx, buscarNotaPorId, id)
	var i FaturamentoNota
	err := row.Scan(
		&i.ID,
		&i.NumSeq,
		&i.Status,
		&i.CreatedAt,
		&i.PrintedAt,
	)
	return i, err
}

const buscarNotaPorNumero = `-- name: BuscarNotaPorNumero :one
SELECT id, num_seq, status, created_at, printed_at FROM faturamento.notas 
WHERE num_seq = $1
`

func (q *Queries) BuscarNotaPorNumero(ctx context.Context, numSeq int64) (FaturamentoNota, error) {
	row := q.db.QueryRow(ctx, buscarNotaPorNumero, numSeq)
	var i FaturamentoNota
	err := row.Scan(
		&i.ID,
		&i.NumSeq,
		&i.Status,
		&i.CreatedAt,
		&i.PrintedAt,
	)
	return i, err
}

const criarNota = `-- name: CriarNota :one
INSERT INTO faturamento.notas DEFAULT VALUES 
RETURNING id, num_seq, status, created_at, printed_at
`

func (q *Queries) CriarNota(ctx context.Context) (FaturamentoNota, error) {
	row := q.db.QueryRow(ctx, criarNota)
	var i FaturamentoNota
	err := row.Scan(
		&i.ID,
		&i.NumSeq,
		&i.Status,
		&i.CreatedAt,
		&i.PrintedAt,
	)
	return i, err
}

const fecharNotaAtomica = `-- name: FecharNotaAtomica :one
UPDATE faturamento.notas
SET status = 'FECHADA',
    printed_at = NOW()
WHERE id = $1
    AND status = 'ABERTA'
RETURNING id, num_seq, status, created_at, printed_at
`

func (q *Queries) FecharNotaAtomica(ctx context.Context, id pgtype.UUID) (FaturamentoNota, error) {
	row := q.db.QueryRow(ctx, fecharNotaAtomica, id)
	var i FaturamentoNota
	err := row.Scan(
		&i.ID,
		&i.NumSeq,
		&i.Status,
		&i.CreatedAt,
		&i.PrintedAt,
	)
	return i, err
}

const listarNotas = `-- name: ListarNotas :many
SELECT id, num_seq, status, created_at, printed_at FROM faturamento.notas
ORDER BY created_at DESC
LIMIT 100
`

func (q *Queries) ListarNotas(ctx context.Context) ([]FaturamentoNota, error) {
	rows, err := q.db.Query(ctx, listarNotas)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FaturamentoNota{}
	for rows.Next() {
		var i FaturamentoNota
		if err := rows.Scan(
			&i.ID,
			&i.NumSeq,
			&i.Status,
			&i.CreatedAt,
			&i.PrintedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const verificarStatusNota = `-- name: VerificarStatusNota :one
SELECT status FROM faturamento.notas 
WHERE id = $1
`

func (q *Queries) VerificarStatusNota(ctx context.Context, id pgtype.UUID) (string, error) {
	row := q.db.QueryRow(ctx, verificarStatusNota, id)
	var status string
	err := row.Scan(&status)
	return status, err
}
